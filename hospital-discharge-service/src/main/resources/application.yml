server:
  port: 8081

spring:
  application:
    name: hospital-discharge-service
  datasource:
    url: jdbc:mysql://localhost:3306/hospital_db
    username: root
    password: yourpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
